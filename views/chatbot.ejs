<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SyncRoom - AI Chatbot</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        #chat-container {
            height: calc(100vh - 200px); /* Adjust based on header/footer */
        }
    </style>
</head>
<body class="bg-gray-900 text-white flex flex-col h-screen">

    <nav class="bg-gray-800/80 backdrop-blur-md shadow-sm px-4 sm:px-6 py-4 flex justify-between items-center border-b border-gray-700">
        <a href="/" class="text-2xl font-bold text-indigo-400">SyncRoom</a>
        <div>
            <a href="/groups" class="text-sm sm:text-base font-medium text-gray-300 hover:text-indigo-400 transition-all">
                Dashboard
            </a>
        </div>
    </nav>

    <main class="flex-1 flex flex-col w-full max-w-4xl mx-auto p-4">
        <div id="error-container" class="mb-4"></div>
        <div id="chat-container" class="flex-1 overflow-y-auto p-4 space-y-6">
            <div id="message-list" class="space-y-6">
                <!-- Messages will be rendered here -->
            </div>
        </div>

        <div class="mt-auto pt-4">
            <form id="prompt-form" class="relative">
                <textarea
                    id="prompt-input"
                    rows="1"
                    class="w-full resize-none bg-gray-800 border border-gray-700 rounded-2xl p-4 pr-16 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition-all"
                    placeholder="Message Gemini..."
                ></textarea>
                <button
                    id="prompt-submit"
                    type="submit"
                    disabled
                    class="absolute right-4 top-1/2 -translate-y-1/2 w-10 h-10 bg-indigo-600 rounded-full flex items-center justify-center disabled:bg-gray-600 disabled:cursor-not-allowed hover:bg-indigo-500 transition-colors"
                >
                    <!-- Send Icon SVG will be injected here -->
                </button>
            </form>
            <p class="text-xs text-center text-gray-500 mt-2">
                Powered by Google Gemini.
            </p>
        </div>
    </main>

    <script src="/js/chatbot.js"></script>
</body>
</html>
